# Handoff — 내부 모델 품질 문제와 미래 과제 분석 구조 재설계

## 다음 세션의 목표

현재 구현된 `nebula` 파이프라인을 내부망의 GPT-OSS 120B에서도 안정적으로 쓸 수 있도록 추출·시간 판정·역량 연결·PJT별 taxonomy 설계를 다시 잡는다. 발표용 성운 화면은 유지하되, 성운의 의미를 일반적인 업무 분야가 아니라 **원문에 근거한 미래 과제군**으로 바꾼다.

이 문서는 문제 진단과 다음 구현 판단에 집중한다. 현재 구현 계약과 운영법은 [docs/nebula-spec.md](docs/nebula-spec.md), [docs/nebula-internal-runbook.md](docs/nebula-internal-runbook.md)를 먼저 참고한다. 원자료의 성격과 한계는 [docs/source-material-brief.md](docs/source-material-brief.md)에 정리되어 있다.

## 사용자가 내부망에서 확인한 문제

내부망 OpenAI 호환 endpoint에 GPT-OSS 120B를 연결해 실행 자체는 되는 것으로 보인다. 그러나 결과 품질은 다음과 같았다.

1. taxonomy 이름과 설명이 영어로 생성된다. 한국 조직 발표 자료에서 그대로 쓸 수 없고, 후공정 DRAM 테스트 분야의 한글 업무 표현과 영문 약어를 함께 보존하지 못한다.
2. 단기·중기·장기 분류가 거의 되지 않는다. 상당수 과제가 시점 미명시로 남는다.
3. 추출된 역량이 매우 적다. 추출된 역량도 미래 과제나 taxonomy에 자연스럽게 붙지 않는다.
4. taxonomy가 ‘미래 과제’처럼 읽히지 않는다. 일반 업무 분야나 기술 키워드 분류처럼 보이며, 조직이 앞으로 무엇을 바꾸려는지를 표현하지 못한다.
5. 따라서 현재 결과만으로는 성운 시각화가 멋있어도 내용에 대한 신뢰가 생기지 않는다.

실데이터·내부 응답·모델 endpoint 정보는 저장소에 없으며 이 문서에도 기록하지 않았다. 다음 세션은 원문을 외부로 복사하거나 로그에 남기지 말아야 한다.

## 이번 세션에서 내린 진단

이 문제는 GPT-OSS 120B의 성능 문제만으로 설명하면 안 된다. 현재 계약 자체가 사용자가 원하는 결과와 어긋나거나 내부 모델에 너무 많은 판단을 한 번에 요구한다.

### 1. 영어 taxonomy는 프롬프트 계약의 결함

[nebula/prompts.py](nebula/prompts.py)의 프롬프트가 전부 영어이고 출력 언어를 한국어로 제한하지 않았다. 모델이 영어 taxonomy를 반환해도 현재 validator는 정상 결과로 받아들인다. 다음 계약을 명시해야 한다.

- `name`, `definition`, `includes`, `excludes`, assignment `reason`, 추출 `label`은 자연스러운 한국어로 작성한다.
- STDF, FA, DPPM, ATE, MBT, BIB, V93000 등 원문의 장비명·제품명·표준 약어는 번역하거나 풀어 쓰지 않고 보존한다.
- 영어만으로 된 taxonomy 이름은 구조 검증에서 거부하거나 검토 경고로 표시한다. 단, 약어 하나가 이름의 일부인 정상 사례는 허용해야 하므로 단순 ASCII 금지는 피한다.

### 2. 시간 분류가 적은 것은 현재 구현상 예상된 결과

[nebula/model.py](nebula/model.py)의 validator는 과제에 `short/mid/long`을 부여하려면 `time_quote` 안에 과제 인용 전체와 정확히 `단기/중기/장기`라는 표지가 함께 있어야 한다. 이것은 추측을 막는 데는 효과적이지만 PPT 구조를 잃은 평문에서는 지나치게 엄격하다.

예를 들어 `단기 목표` 제목 아래의 `테스트 절차 자동화` 불릿은 사람이 보면 단기 과제지만, 불릿 자체에 `단기`가 없으면 현재 계약으로는 `unknown`이다. `하반기`, `내년`, `향후`, `중장기`, `3년 이내`도 현재 단·중·장기로 인정되지 않는다.

시간을 억지로 채우는 방식도 피해야 한다. 원자료는 10년 시계열이 아니라 한 번 수집한 개인의 근미래 서술이다. 다음 구조가 적절하다.

- `time_expression`: 원문 표현을 그대로 보존한다. 예: `하반기`, `단기`, `중장기`, `향후 3년`.
- `time_source`: `inline`, `section_heading`, `document_context`, `unspecified`처럼 근거 종류를 보존한다.
- `horizon`: 공통 기준으로 안전하게 매핑되는 경우만 `short/mid/long`; 그렇지 않으면 `unknown`.
- PPT→text 단계에서 가능하다면 제목/불릿 관계를 잃지 않는 구조화 입력을 받는다. 이미 평문만 있다면 제목 범위를 복원하는 별도 전처리 단계를 둔다.
- `중장기`를 임의로 중기 또는 장기로 하나만 선택하지 않는다. 원문 표현을 먼저 보존하고 화면 매핑 정책을 별도로 정한다.

### 3. 역량이 적은 것은 직접 연결 계약이 너무 엄격하기 때문

현재 `EXTRACT` 호출은 한 사람의 글에서 미래 과제, 보유/필요 역량, 과제-역량 링크를 동시에 생성한다. 또한 `relation_quote` 하나가 과제 인용과 역량 인용을 모두 포함해야 링크를 허용한다.

원자료 양식에서는 `커리어 회고 및 확보 역량`과 `미래 업무`가 서로 다른 섹션일 수 있다. 실제로 역량을 명시했어도 과제와 같은 연속 문장에 있지 않으면 링크가 거의 생기지 않는 것이 정상이다. 이 때문에 화면에서 역량이 적게 보이며, 역량이 없는 사람처럼 오해될 수 있다.

다음 세션에서는 판단을 분리한다.

1. 미래 과제를 독립 추출한다.
2. 보유 경험·보유 역량·확보 필요 역량을 독립 추출한다.
3. 시간 근거를 별도 판정한다.
4. 과제와 역량의 연결을 별도 단계에서 판단한다.

연결은 최소 세 수준으로 저장하는 방향을 검토한다.

- `explicit`: 작성자가 과제와 역량을 직접 연결했다.
- `contextual_candidate`: 같은 사람의 다른 섹션이지만 업무 대상·맥락이 맞아 검토 후보로 제시한다.
- `unlinked`: 같은 사람이 썼다는 사실 외에 연결 근거가 없다. 개인 역량으로는 보존하지만 과제에는 붙이지 않는다.

`contextual_candidate`는 확정 사실처럼 발표 집계에 넣으면 안 된다. 검토 화면에서 승인한 연결만 최종 발표에 반영하거나, 후보임을 분명히 구분해야 한다.

### 4. taxonomy의 의미가 잘못 정의되어 있다

현재 `TAXONOMY`와 `CONSOLIDATE`는 카테고리를 `WORK SUBJECTS`로 만들라고 지시한다. 이 계약으로는 `데이터 분석`, `품질 관리`, `자동화` 같은 업무 분야 분류가 생성되기 쉽다. 사용자가 원하는 성운은 일반 분야가 아니라 **사람들이 앞으로 만들고 바꾸려는 미래 과제군**이다.

좋은 미래 과제군 이름은 가능하면 다음을 포함한다.

> 업무 대상 + 변화 방향 또는 도달 상태

합성 예시:

- `데이터 분석` → `테스트 데이터 분석의 자동화·재현성 확보`
- `품질 관리` → `불량 징후의 조기 탐지와 원인 진단`
- `지식 관리` → `개인의 검증 경험을 재사용 가능한 절차로 전환`

오른쪽 표현도 원문 과제들이 실제로 그 변화를 말할 때만 허용한다. 원문에 없는 `지능화`, `고도화`, `플랫폼화`를 발표용으로 추가하면 안 된다. 희소하지만 구체적인 미래 과제는 넓은 분야 밑에 억지로 넣지 말고 독립 과제군 또는 미분류로 유지한다.

권장 계층은 다음과 같다.

```text
PJT
  └─ 미래 과제군
       ├─ 개인별 미래 과제 언급과 원문
       ├─ 시간 표현과 근거
       └─ 연결이 승인된 보유 경험·필요 역량
```

PJT별 taxonomy 독립성은 유지한다. 서로 다른 PJT가 같은 단어를 쓴다는 이유만으로 같은 과제군으로 합치지 않는다. 전사 관점의 공통 축이 필요하면 PJT별 과제군이 안정된 뒤 별도의 상위 해석 단계로 다룬다.

## 이미 효과가 확인된 것

- 사람별 exact quote 검증, 안정적인 ID, 검증된 응답만 캐시하는 방식은 유지할 가치가 있다.
- 정정 내용을 누적 보존하고 원문이 바뀌면 오래된 정정을 버리는 흐름은 구현·테스트되어 있다.
- PJT별 taxonomy, 미분류 허용, 검토 후 승인 화면 생성은 계속 필요하다.
- 사람 유사도 → 과제 분화 → PJT별 성운 → 시간 → 역량의 다섯 장면 발표 스토리는 코드에 연결되어 있다.
- `nebula.html`은 메인 발표 화면이며 `matrix.html`과 `review.html`이 같은 실행 결과를 사용한다.
- 과제 0개·승인 0개·자동 발표·원문 근거·특수문자 입력을 포함한 브라우저 경계도 검증되어 있다.

현재 발표 구현 관련 커밋과 상세 변경은 git history를 참고한다. 최신 검증 시점에는 전체 테스트 329개와 두 브라우저 시나리오가 통과했다.

## 다음 세션에서 구현할 권장 순서

1. 내부 모델이 반환한 결과를 외부 반출하지 않고 내부 운영자가 확인할 수 있도록, 실패 유형별 소수 표본 평가표를 먼저 정의한다. PJT별 2~3명 정도에서 과제 누락·창작, 시간 근거, 역량 누락, 링크 정확성, 과제군 적합성을 사람이 체크하게 한다.
2. `EXTRACT`를 과제 추출과 역량 추출로 나누거나, 적어도 모델 호출 계약을 분리한다. GPT-OSS 120B에서는 짧은 한국어 지시와 각 단계의 JSON 예시를 사용한다.
3. 입력 스키마에 제목/불릿 구조를 실을 수 있는지 확인한다. 가능하면 `text`만 보내지 말고 섹션 메타데이터를 추가한다. 불가능하면 제목 범위 복원 전처리를 구현한다.
4. 시간 스키마를 `time_expression`, `time_source`, `horizon`, `time_quote`로 확장한다. 기존 결과와 캐시 계약 버전을 올리고 마이그레이션 또는 새 out 사용을 문서화한다.
5. taxonomy 프롬프트를 한국어로 다시 쓰고, `미래 과제군`의 정의와 좋은/나쁜 합성 예시를 넣는다. 출력 언어 검증을 추가한다.
6. 역량 링크를 확정 링크와 검토 후보로 분리한다. 발표 집계에는 승인된 확정 링크만 기본 포함한다.
7. 내부 운영자가 모델 A/B 결과를 같은 원문 표본에서 비교할 수 있는 품질 리포트를 만든다. 호출 성공률만으로 개선을 판정하지 않는다.
8. 기존 계약 테스트, 약한 모델을 흉내 낸 fixture 테스트, 전체 테스트, 브라우저 검사를 실행한다. 실제 내부 모델 의미 품질은 내부망에서만 평가한다.

## 피해야 할 접근

- 프롬프트를 더 길고 복잡하게 만드는 것만으로 해결하려 하지 않는다. 한 호출의 판단 종류를 줄이는 것이 우선이다.
- 미분류 비율을 낮추기 위해 모델이 시간이나 역량 연결을 추측하도록 만들지 않는다.
- 작성자 수를 중요도·준비도·역량 수준으로 해석하지 않는다.
- 보유 역량과 필요 역량의 차이를 조직의 실제 gap 수치로 표시하지 않는다. 이는 자기서술의 언급 차이다.
- taxonomy 카테고리 수를 고정하지 않는다.
- 발표 화면의 성운 배치가 분류 품질을 증명한다고 설명하지 않는다.
- 내부 실데이터나 모델 응답을 저장소·외부 로그·스크린샷에 남기지 않는다.

## 현재 저장소 상태

- 저장소/워크트리: `/Users/winter/Documents/Codex/2026-09-11/future-task-implementation`
- 브랜치: `codex/future-task-capability-map`
- 이전 세션에서 원격 브랜치까지 push되어 있다. 이 handoff 커밋 이후 원격 SHA를 다시 확인한다.
- 기존 발표 프로토타입과 과거 HTML은 유지한다.
- 합성 출력은 `data/nebula-demo/`에 있으며 gitignore 대상이다.

## 다음 세션에서 먼저 읽을 파일

- `HANDOFF.md`
- `docs/source-material-brief.md`
- `docs/nebula-spec.md`
- `docs/nebula-internal-runbook.md`
- `nebula/prompts.py`
- `nebula/model.py`
- `nebula/pipeline.py`
- `nebula/render.py`
- `tests/test_nebula_pipeline.py`
- `tests/nebula_browser.cjs`
- `tests/nebula_presentation.cjs`

## Suggested skills

- `$implement` — 새 스키마와 단계별 호출을 실제 코드로 완결할 때 사용.
- `$tdd` — 과제·시간·역량·taxonomy의 새 공개 계약을 먼저 fixture로 고정할 때 사용.
- `$code-review` — 구현 후 기존 근거 추적성과 발표 흐름의 회귀를 점검할 때 사용.
- `$domain-modeling` — `미래 과제`, `미래 과제군`, `역량`, `연결 후보`, `시간 근거`의 용어와 경계를 먼저 확정할 때 사용.
- `$writing-for-agents` — 내부망 운영 에이전트 문서와 프롬프트를 갱신할 때 사용.

